# ========== 仅前端（Vite）==========
# 复制为 .env 本地使用。不要在此文件填写管理员密码或腾讯云密钥（那些只在 server 环境变量里配）。
#
# 留空 = 请求当前页面同源下的 /api（一体部署、或 dev 时走 Vite 代理）
# VITE_API_BASE=
#
# 分域部署（如 Vercel 前端 + 1Panel 后端）：在 Vercel「Environment Variables」里设置 VITE_API_BASE=https://你的API域名
# 详见 DEPLOY.md「GitHub + Vercel 与腾讯云 1Panel」
#
# 旧版：仅服务端 API_TOKEN、且未启用管理员 JWT 时，保存可带静态 Bearer
# VITE_API_TOKEN=

# 为 true 时登录成功不再把 JWT 写入 localStorage，仅依赖服务端 httpOnly Cookie（须与 VITE_API_BASE 跨域 + 服务端 CORS_ORIGIN / Cookie 域配置一致）
# VITE_AUTH_HTTPONLY_COOKIE=true

# 与 server 的 COS_PUBLIC_BASE 一致（自定义域/CDN 时）；默认桶域名示例（新加坡）：
# VITE_COS_PUBLIC_BASE=https://mikujar-1310791405.cos.ap-singapore.myqcloud.com
